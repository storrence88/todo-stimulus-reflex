<div id='list-<%= list.client_id %>' class="card mb-3">
  <div class="card-header">
    New List
  </div>
  <div class="card-body">
    <%= bootstrap_form_with model: list do |f| %>
      <%= f.hidden_field :client_id %>
      <%= f.text_field :name %>
      <%= f.primary %>
    <% end %>
  </div>
</div>