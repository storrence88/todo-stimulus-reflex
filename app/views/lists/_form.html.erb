<div id='new-list' class="card">
  <div class="card-header">
    New List
  </div>
  <div class="card-body">
    <%= bootstrap_form_with model: list do |f| %>
      <%= f.text_field :name %>
      <%= f.primary %>
    <% end %>
  </div>
</div>