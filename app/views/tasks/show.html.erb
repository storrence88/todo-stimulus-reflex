<h1><%= @task.name %></h1>

<% if @task.assignee %>
  <p class='text-muted'>Assigned to: <%= @task.assignee_name %></p>
<% end %>

<% if @task.complete? %>
  <p class='text-muted'>Completed by: <%= @task.completer_name %></p>
<% end %>

<%= link_to 'Back to lists', root_path %>

<hr>

<h4 class='mb-4'>Comments</h4>
<div id="comments" data-controller='comments' data-comments-task-id='<%= @task.id %>'>
  <%= render @task.comments %>
</div>

<%= render 'comments/form', new_comment: @new_comment %>